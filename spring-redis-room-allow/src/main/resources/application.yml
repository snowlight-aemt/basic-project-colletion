spring:
  jpa:
#    show-sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  datasource:
#    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/Study/Tools/h2_db/test-room-allow
    username: sa
    password:

logging:
  level:
    sql: trace

