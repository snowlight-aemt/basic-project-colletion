spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:test-room-allow
#    username: sa
#    password:
logging:
  level:
    sql: debug

val:
  env: dev